#include <stdio.h>

int main(void) {

	printf("Hello World\n");
	printf("%d\n", 0657);
	// 정수와 실수 표현 공부
	return 0;
	/* 기능이 제대로 수행되면 보통은 0을 리턴하는 것으로 
	 보통 함수가 기능을 수행한 후에 문제가 있을 경우 FALSE 혹은 다른 값을 리턴 */
}